# What is left, and why it is Al's

**Date:** 21 August 2026
**Companion to:** `docs/internal/audit-2026-08-20.md`, which records what was done
**Check the claims here with:** `bash scripts/audit-verification.sh`

Fourteen items cannot be finished by Claude. They fall into five kinds of blocked, and
the kind matters more than the item, because it tells you what unblocks it.

| Kind of blocked | Items | What unblocks it |
|---|---|---|
| Your judgment about the methodology | 1, 2, 3, 4, 13 | A decision, then some of it is mechanical |
| Yours to author, by design | 5 | Time in a quiet block |
| Outside this repository | 6, 7 | Another person answering |
| A one line decision | 8, 9, 10, 11, 14 | Ten seconds each, then Claude can execute |
| A daemon that was not running | ~~12~~ resolved 23 August | Start Docker |

Items 13 and 14 were added on 23 August, after the 1.0.3 methodology work. Item 12 is
kept rather than deleted, because why it was blocked is part of the record and the
result of finally running it is worth reading.

**Read the dependency order before picking one up.** Item 1 gates item 5, which is
the largest piece of remaining work. Starting item 5 before deciding item 1 means
writing formulas against a fixture whose inputs are wrong.

```
1. DEC-006, the pool ──┬──> 5. internal/depth beyond MidPrice
                       └──> 4. the two C_max terms
2. criticalDelta ─────────> 5. ComputeManipulationCost and ComputeAssetRisk
                       └──> 7. the contract freeze
6. Reflector's window ───> the oracleResistance default
6. Blend's parameters ───> the C_max defaults, and the DoD's "name the source"
7. Frontend answers ─────> the contract freeze
3. Methodology structure ─> the Deliverable 1 DoD
```

---

## 1. DEC-006: the golden fixture is missing an AMM pool

**Why it is not Claude's.** Correcting it means editing the golden fixture's inputs,
which invalidates every expected value in it, and editing methodology section 10.4,
which is the paid deliverable. `internal/conformance/fixture.go` says it in its own
header: do not adjust these numbers to match the code, adjust the code to match these
numbers. The same logic protects the inputs. If Claude recomputes them, the one
safeguard this methodology has is gone.

**One honest admission first.** DEC-006 section 2 already contains Claude's computed
consequences: depth becoming 101.88 / 103.61 / 106.43 and manipulation cost becoming
127.03 / 279.29 / 502.07 / 1290.56. Having those numbers in front of you weakens the
independence of your own recomputation, and that is a cost that was already paid to
make the finding legible. The mitigation is not to pretend otherwise: derive them
again from the raw inputs below, and if your figures differ from Claude's, treat
Claude's as suspect and find out why. An independent derivation from the same inputs
still validates that the formula was applied correctly, which is most of what the
check was for.

**The raw inputs, so you can start from them and not from the answers.**

```
Pool 27480d0483c8320ba4a707797526ffd67118e841491e0cbeb66db697bb66cccb
  ReserveQuote (USDC)  = 16.3389179
  ReserveBase  (USTRY) = 15.4791416
  FeeBP                = 30

Book, unchanged from the current fixture
  one ask  price_r {266843207, 2500000}, amount 1.2185312
  one bid  price_r {1057, 1000},         amount 0.0001000
```

Formulas: methodology section 5 for the AMM, section 6 for the combination, section 7
for manipulation cost.

**How to resolve, three roads.** They are set out in DEC-006 section 6. Short form:

- **A. Correct the fixture and the claim.** Add the pool to `GoldenSnapshot()`,
  recompute the depth and manipulation tables by hand, rewrite section 10.4 to say
  what is true: zero through the orderbook, ~127 USDC through the combined price, and
  the attacker paid 5.35 by using the orderbook alone. Raises `MethodologyVersion`.
- **B. Keep the fixture and relabel it.** Call it an orderbook-only fixture, state
  that pools are excluded, add a second fixture with the pool. Cheaper, keeps your
  hand computed numbers valid, but leaves the "zero" sentence standing beside a
  fixture that assumes a venue away.
- **C. Split the model by venue, not only by side.** Report the cost to move the
  orderbook, the cost to move the pool, and the cost to move the combined price. The
  oracle-relevant number is then whichever venue the oracle reads.

**Recommendation: A and C.** A is not optional, because a false headline number is
the one thing review will not survive. C is what turns this from an erratum into the
most interesting result in the deliverable: that the cheapest attack path is the one
ignoring half the liquidity, and that combining venues can understate oracle risk.
Nothing in this ecosystem says that today.

**One check worth ten minutes before you rewrite anything.** The finding rests on the
claim that pool reserves cannot change without a transaction associated with the pool.
That is an argument from how Stellar works, not from the data. If some reserve
changing event does not produce such a transaction, the inference fails. Confirm it
against the protocol documentation, not against Horizon.

---

## 2. `criticalDelta`: 0.5 or 1.0

**Why it is not Claude's.** Changing it means editing methodology section 9 and
raising `MethodologyVersion`.

**What it costs to leave open.** The contract cannot be frozen, and the frontend has
been told to read the value from the response and hardcode neither. It also changes
`C_max`, so item 5 cannot produce a final number without it.

**The disagreement.**

| Source | Value |
|---|---|
| `docs/api/keel-openapi.yaml` and every example | 0.5 |
| `keel-methodology-core.md` section 9, `P_critical = 2 x P0` | 1.0 |
| `DefaultParams()` in `internal/conformance/fixture.go` | 1.0 |

**The argument for 0.5.** On the golden fixture, delta 1 makes the target
unreachable, so `MC` there is not a cost-to-reach at all, and using it as a term in
`C_max` multiplies a meaningless number. At delta 0.5 the target is reachable at zero
cost, `C_max` comes out zero, and zero is the right conservative answer for an asset
in that state.

**How to resolve.** Decide, then this is mechanical and Claude can do it in one pass:
methodology section 9, `DefaultParams()`, the contract if you choose 1.0, the mocks,
and `MethodologyVersion`. Fifteen minutes.

---

## 3. The methodology file structure, and four documents that do not exist

**Why it is not Claude's.** This is the intellectual work of Deliverable 1, not
typing. The DoD promises that an outsider can reproduce one number from scratch using
the documents alone. If Claude writes them, that promise is false in a way nobody can
see from the outside, which is worse than the gap.

**The structure decision.** `docs/methodology/README.md` section 3 lays out two roads
and recommends road 2: keep the current structure, amend the DoD in
`Keel_Deliverable_1_Rencana_Eksekusi.md` section 6 to describe what actually exists.
Road 1 splits `keel-methodology-core.md` into ten numbered files and renumbers
`09-flag-dan-band.md`, which is referenced from six places. Take road 2 unless you
have a reason.

**The four documents that are missing on either road**, with what each has to contain
to satisfy the DoD:

| Missing | Must contain |
|---|---|
| Data sources | Horizon versus Hubble, what each can and cannot answer, and the fact that Horizon serves no historical orderbook state |
| Pair selection | Decision D-1: depth per pair, how the primary pair is chosen, and path payments named as a known limitation |
| Supporting metrics | Decisions D-4 to D-6: what a genuine trade is, the holder concentration population, which supply the volume ratio uses. The exclusion list is what makes these auditable |
| Validation protocol | The three layers in the execution plan section 4, and the results table. This is the "cross-validation passed on 50+ sample ledgers" the SOW promised |

The limitations file already exists as methodology section 12, and it is the section
a reviewer will look for first. It is honest. Leave it that way.

---

## 4. P1-15: the two `C_max` terms

**Why it is not Claude's.** Methodology section 9 requires both the liquidation limit
and the manipulation limit to be reported rather than only their minimum, but
`domain.AssetRisk` carries only the minimum. Adding the fields means naming them, and
a name in the output type is a methodology choice. It is also now blocked behind item
1, because the pool changes what `C_max` means for this asset.

**Why it does not block a handoff.** The change is additive. A frontend built today
will not break when the fields appear.

**How to resolve.** After item 1: choose two names, add them to `domain.AssetRisk`,
add them to the contract, regenerate the mocks with `make api-mocks`. Claude can do
all of that once the names are yours.

---

## 5. T4: `internal/depth`

**Why it is not Claude's.** It is the red zone, and that is the point of the zone.
You have to be able to defend every number in it to a reviewer or a funder. It is
also enforced mechanically, by the `Edit` and `Write` denials in
`.claude/settings.json` and by `.claude/hooks/lindungi-zona-merah.sh` for Bash.

**What is already waiting for you.** `internal/conformance/golden_test.go` fixes the
API surface, so there is nothing to design:

```go
depth.MidPrice(snapshot)                                  (decimal.Decimal, domain.PriceSource)
depth.ComputeDepth(snapshot, P0, deltas)                  ([]domain.DepthPoint, error)
depth.ComputeManipulationCost(snapshot, P0, deltas)       ([]domain.ManipulationPoint, error)
depth.ComputeAssetRisk(snapshot, params)                  (domain.AssetRisk, error)
```

**The sequencing that item 1 forces.** `MidPrice` is unaffected by the pool, because
the fallback ladder stops at case 1 as soon as the book has two sides. So `MidPrice`
is safe to write today, before deciding item 1. `ComputeDepth` is not: its expected
values change if the pool goes into the fixture. Write `MidPrice`, then decide item 1,
then continue.

**How to resolve.**

```bash
make conformance              # red until internal/depth has a body
```

Suggested order and the test that closes each step:

1. `MidPrice` → `TestMidPrice` passes. Safe now.
2. Decide item 1.
3. `ComputeDepth`, SDEX only first → `TestComputeDepth`, then `TestInvarianMonotonisitas`.
4. AMM and the combination → the discriminating test in methodology section 6: a
   fixture whose pool price sits 5 percent above `P0`, asked for depth at 2 percent,
   must return `fromAmm` exactly zero. An implementation that sums separately returns
   non-zero.
5. `ComputeManipulationCost` → `TestComputeManipulationCost`. Needs item 2 decided.
6. `ComputeAssetRisk` → the flag, band, metadata, and determinism tests.

When it passes: delete the `//go:build conformance` line in `golden_test.go`, delete
the separate `conformance` target in the `Makefile`, and delete `continue-on-error`
from the conformance job in CI. All three exist only because that package is empty,
and each one says so in a comment.

**Claude's role in there stays.** Reading, running tests, showing edge cases, asking
questions. `/teach` for a concept, `/review-mine` after you write. Two questions are
already waiting in `audit-2026-08-20.md` section 3, and a third in DEC-006 section 4
about whether venues need separating.

---

## 5b. What the golden fixture should call itself

**Why it is not Claude's.** It is a claim about what the data is, and it sits inside
the fixture the whole methodology is checked against.

`GoldenSnapshot()` labels itself `DataSourceHorizon`. The contract and DEC-003
section 2.6 label the same book `trades-implied`, on the grounds that it was
reconstructed from on-chain operations rather than read from an orderbook snapshot.
`TestMetadataWajib` locks in whichever value you choose, so item 5 depends on it.

**And neither label is exactly right, which is the interesting part.** The
`DataSource` enum defines `trades-implied` as reconstructed "from trades that actually
executed". This book was reconstructed from `manage_sell_offer` and `manage_buy_offer`
**operations**, which is not the same thing and is arguably a stronger source: an
offer proves liquidity that was *posted*, a trade proves only liquidity that was
*consumed*. The methodology leans on that distinction everywhere else, so collapsing
it here is odd.

**How to resolve, pick one.**

- Broaden the enum description so `trades-implied` covers any reconstruction from
  ledger operations, and note that offer-derived reconstruction is the stronger case.
- Add a fourth value, something like `offers-implied`, and say why it ranks above
  `trades-implied` in confidence.
- Keep `horizon` on the fixture and accept the label is loose, which is the one option
  that leaves a reader misinformed.

The first is cheapest and honest. The second is the most accurate and costs a contract
change plus a mocks regeneration.

---

## 6. DEC-001 items 3, 4, and 5: sources this repository does not have

**Why it is not Claude's.** These need primary sources that are statements by other
parties, not data on the ledger.

| Item | What it blocks | Where to get it |
|---|---|---|
| The exact collateral amount | The Deliverable 2 report. Sources say 153,003 and 149,876 USTRY | On-chain: the borrower account's operations around 2026-02-22 00:25 |
| Reflector's VWAP window length | Methodology section 8's default of 15 minutes is an assumption, and `oracleWindowSeconds` in the contract rests on it | Reflector's own statement or documentation. Script3 said "no other trade in 15 minutes", which is not the same claim |
| YieldBlox pool risk parameters | The `C_max` defaults, and the DoD's requirement to name their source | The Blend V2 pool configuration, plus Script3's statements of 22 and 23 February 2026 |

Item 4 is the one worth chasing first. The whole `oracleResistance` metric compares a
manipulation cost against genuine volume inside a window, and right now that window is
a guess documented as a guess. A confirmed number turns an assumption into a
measurement.

---

## 7. The frontend questions

**Why it is not Claude's.** Another person has to answer them. DEC-003 freeze
condition 4 cannot be closed from inside this repository, and it is now the only one
of the four still open.

**How to resolve.** Send one link, `docs/api/mocks/README.md`, and the five questions
in DEC-003 section 6. Then record each answer on its `Answered:` line with the date
and what changed, including when the answer is "no change needed". An answer that
leaves no trace cannot be told apart from a question never asked, which is why
`scripts/audit-verification.sh` counts those lines rather than trusting anyone's memory.

The freeze condition flips to MET on its own once every line is filled.

---

## 8 to 11. Four one line decisions

Each of these is yours to make and then mechanical to execute. None needs more than a
sentence from you.

**8. Repository visibility.** DEC-004 says private until `internal/depth` passes the
fixture. The trigger is `make conformance` passing without a build tag. Before
flipping it, `docs/context/Keel_SoW.pdf` and `docs/internal/` have to come out, and
`git rm` alone is not enough because both are already in the history: either rewrite
with `git filter-repo` while nobody has cloned, or start a clean public repository and
keep this one as a private archive. The verification script shouts if the repository
goes public while either is still present.

**9. `keel-bootstrap.sh`.** It carries its own copies of `CLAUDE.md`, `README.md`, and
the zone files, and they have already drifted; the `CLAUDE.md` inside it is the
pre-English version. It has done its job. Recommendation: `git rm keel-bootstrap.sh`.
The history keeps it either way. It was not deleted because deleting your file is not
Claude's call.

**10. The Go module path.** The repository is now `Keel-Official/keel-backend` and the
module is still `github.com/Keel-Official/keel`. Renaming touches `go.mod` and six
import lines. Leaving it works because GitHub redirects, but it is a footgun for
anyone running `go get`. Either is defensible; decide rather than drift.

**11. Indonesian identifiers.** `belumSiap`, `paketMurni`, `cocokTerlarang`,
`TestArchTanpaImportTerlarang`, and others. DEC-005 left them alone deliberately,
because `Makefile` and `ci.yml` both select tests with `-run TestArch`, and a rename
that misses one of them silently stops running the architecture tests. If you want
them renamed, the safe order is: rename the selectors in `Makefile` and `ci.yml`
first, watch CI go red, then rename the tests and watch it go green. A test that
silently stops running is worse than a test named in the wrong language.

### Answered 23 August 2026

**9. Deleted.** `git rm keel-bootstrap.sh`. The history keeps it, and DEC-005 carries a
note that its exemption from translation no longer has a subject.

**10. Renamed** to `github.com/Keel-Official/keel-backend`, matching the remote. It came
to seven import lines rather than six, because `golden_test.go` imports two packages.
`go build`, `go vet`, and `go test` are all green on the new path.

One thing the rename surfaced rather than caused: `go mod tidy` now fails, because
`golden_test.go` imports a package directory that holds no `.go` file, and tidy tries to
resolve it as an external module. It failed the same way before the rename. It is P0-3
wearing a different hat, and it will clear itself the moment that package has a body.

**11. Not renamed, left in Indonesian.** Al's instruction was not to rename identifiers,
so `belumSiap`, `paketMurni`, `cocokTerlarang`, and `TestArchTanpaImportTerlarang` stay
as they are, and DEC-005 keeps its deliberate exception exactly as written. The reading
taken here is the literal one, that no rename happens. If what was meant was the
opposite, that the repository should carry no Indonesian identifiers at all, say so and
the safe order above still applies: selectors in `Makefile` and `ci.yml` first, watch CI
go red, then the tests, and watch it go green.

**8 is still open**, and it is the only one of the four that was not a one line decision.
It waits on `make conformance` passing.

---

## 12. Applying the migration

**Why it was not done.** The Docker daemon was not running, and there is no local
`psql`. The SQL in `migrations/0001_core.sql` has never been executed. Only the bash
syntax of `scripts/migrate.sh` was checked.

**How to resolve.** This is the real test, and the second command matters more than
the first:

```bash
make up && make migrate      # expect: apply 0001_core.sql, then "1 applied"
make migrate                 # expect: "skip", then "0 applied"
```

If the second run applies anything again, the `schema_migrations` bookkeeping is
broken and that is a bug worth finding now rather than on the day a second migration
exists.

**RESOLVED 23 August 2026.** The daemon was running, so this was run. Both halves
hold.

```
make migrate     skip 0001_core.sql        0 applied, 1 already present
make migrate     skip 0001_core.sql        0 applied, 1 already present   (stable)
```

The first half was checked without touching the `keel` database, by applying
`0001_core.sql` to a scratch database created and dropped for the purpose. It
creates `assets`, `metrics`, and `runs` cleanly on an empty database. The
`metrics` CHECK constraints accept `trades-implied` and the three price sources,
which is finding P1-4 confirmed fixed against the running server rather than
against the file.

One property worth naming, because it cuts both ways. Every statement in
`0001_core.sql` uses `IF NOT EXISTS`, so applying it twice is silent rather than
loud. Together with the bookkeeping that is correct and safe. On its own it means
a broken `schema_migrations` table would not announce itself, so the bookkeeping
is the only thing standing between a skipped migration and a silent one. That is
the same failure mode `migrate.sh` was written to kill, now concentrated in one
place instead of two.

---

## 13. `OracleResistance` went back to a scalar

**Added 23 August 2026.** The cross-check at the end of this document printed
`unaccounted: P1-6`, which is what it exists to do. This item is the required entry.

**What happened.** The 1.0.3 rewrite of `internal/domain/types.go` replaced the
`OracleResistance` struct with `OracleResistance *decimal.Decimal`, and changed the
quantity along with the shape. Methodology section 8 now defines
`MR = MC_orderbookOnly(P_target) + V_genuine(W)`, a sum, where the field it replaced
held a ratio plus the two inputs the ratio was computed from.

**Why it is not Claude's.** DEC-003 section 8.2 closed this in favour of the object,
and the reasoning was not about taste. A ratio has two states that a scalar cannot
carry: `GenuineVolume` of zero makes it undefined, and a ratio built on a
`ManipulationCost` whose `Reachable` is false is a meaningless number. The object made
both readable and set `Ratio` to nil. Reversing a recorded decision, and redefining
the metric from a ratio to a sum, is a methodology judgment.

**One thing worth noticing about the sum.** It is not the same measurement. A ratio
below 1 says the attack is cheaper than the genuine trading it has to hide inside. A
sum says how much capital the whole defence represents, and it no longer answers the
question the metric was named for. Both are defensible quantities. They are not
interchangeable, and only one of them is what `oracleResistance` means in the frozen
contract and in five mock responses.

**How to resolve, pick one.**

- Keep the object and the ratio. Revert the field, keep the `orderbookOnly` term in
  the numerator, which is the genuine improvement inside this change. Nothing in the
  contract moves.
- Keep the sum and rename it. It is a different metric, so give it a different name
  and say in section 8 why the ratio was dropped. Costs a contract change, a mocks
  regeneration, and a `MethodologyVersion` note.

**And a note on how this was found.** The check for P1-6 matched the exact column
alignment of the old struct field, so when the scalar came back with different spacing
the check went green and reported the defect gone. That is the fifth check in this
repository defeated by the thing it was checking, and the first that produced a false
negative rather than a false positive. It now matches the declaration instead of its
layout.

---

## 14. The storage schema lags methodology 1.0.3 by six columns

**Added 23 August 2026, found while closing item 12.**

The `metrics` table was checked against the running server, column by column,
against `domain.AssetRisk` as 1.0.3 leaves it:

```
MISSING  pool_spot_price
MISSING  price_divergence_pct
MISSING  manipulation_cost_combined
MISSING  manipulation_cost_orderbook_only
MISSING  max_safe_collateral_liquidation
MISSING  max_safe_collateral_manipulation
```

**Four of the six are mechanical.** `pool_spot_price`, `price_divergence_pct`,
and the two `C_max` terms have names already chosen in the output type, so a
`0002` migration writing them needs nothing from you.

**Two of them wait on a decision that is not this item's.** Whether
`manipulation_cost` stays as the combined ladder and gains a sibling, or is
replaced by two columns and dropped, is the same question the API contract has to
answer, so both should be settled in one pass rather than migrated twice.

**And the schema is a third witness in item 13.** `oracle_resistance` is `jsonb`,
which is the object form. The contract holds the object, the five mock responses
hold the object, and the storage column is shaped for the object. Only
`internal/domain` now holds a scalar. That is not evidence for either road, but it
does say what the scalar costs: three places change, not one.

---

## What is not on this list

Everything in `audit-2026-08-20.md` that flipped to NOT is done and verified. The
count as of this document is **18 claims proven and 27 not**.

Those 18 split cleanly, and the split is checkable rather than a judgment call.

**Seven state a supporting fact and nothing else.** Their partner finding is already
fixed, or they were never a defect on their own, so they stay PROVEN because the fact
stays true: **P1-5, P1-7, P1-12, P1-21, P1-24, P1-27, P1-31.** None of them is work.

**The other eleven all map to an item in this document.** That mapping is the point:
if the script ever shows a PROVEN line that appears nowhere below, either a new
finding has appeared or this document has gone stale.

| Finding | Item |
|---|---|
| P0-2, P0-3 | 5, the red zone is empty so the fixture tests nothing |
| P1-8 | 4, a field the contract now has and the methodology still wants doubled |
| P1-13, P1-14 | 2, `criticalDelta` |
| P1-15 | 4, the two `C_max` terms |
| P1-32, P1-33 | 5b, what the fixture calls itself |
| P1-34, P1-35 | 1, the missing pool |
| P2-7 | 3, the methodology structure |
| P1-6 | 13, `OracleResistance` back to a scalar. Appeared after this document was written |

To re-run that cross-check yourself:

```bash
bash scripts/audit-verification.sh 2>&1 | grep PROVEN | awk '{print $1}' | while read id; do
  grep -q "$id" docs/internal/handoff-2026-08-21.md || echo "unaccounted: $id"
done
```

**It should print nothing.** Every PROVEN id is named in this document: eleven in the
table above, seven in the list of supporting facts. Any id it does print is a finding
that appeared after this document was written, and it needs an item adding here.

A note on why the promise is worded that way. The first version of this paragraph
said the check should print the seven supporting facts, which was wrong the moment
their ids were written into this file: the check searches this file, so naming them
here satisfies it. That was the fourth time in two days a check in this repository
was defeated by the document it was checking, after the TODO-FIXTURE marker, the mock
freshness diff, and the corrected ratio in DEC-001. The pattern is worth keeping in
mind whenever you write one: ask what the check reads, and whether the thing it reads
can be changed by describing it.
